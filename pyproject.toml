[build-system]
requires = ["setuptools>=64", "setuptools_scm[toml]>=6.2", "wheel"]
build-backend = "setuptools.build_meta"


[project]
name = "OpenbindInitialRefiniment"
authors = [{name = "Conor F Wild", email = "c.wild@diamond.ac.uk"}]
dynamic = ["version", "description"]
dependencies = [
    "numpy",
    "scipy",
    "joblib",
    'gemmi'
 ]
dynamic = ["version"]
